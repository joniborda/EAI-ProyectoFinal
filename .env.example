# ConfiguraciÃ³n de PostgreSQL

DB_HOST=localhost
DB_USER=postgres
DB_NAME=postgres
DB_PORT=5432
DB_PASSWORD=password_123

# Tabla y columnas de Ã³rdenes (coincide con README)
ORDERS_TABLE=orders
ORDERS_DATE_COL=created
ORDERS_LINE_ITEMS_COL=line_items

# Claves dentro de cada objeto en line_items (JSONB)
ITEM_PRODUCT_ID_KEY=product_id
ITEM_QUANTITY_KEY=quantity

# Directorio para modelos
MODEL_DIR=models
